metadata:
spec:
  restartPolicy: Never
  containers:
  - args:
      - dask-worker
      - --nthreads
      - '2'
      - --no-bokeh
      - --memory-limit
      - 6GB
      - --death-timeout
      - '60'
    image: gcr.io/dask-demo-182016/dask-tutorial-notebook:latest # TODO: pin
    name: dask-worker
    securityContext:
      capabilities:
        add: [SYS_ADMIN]
      privileged: true
    resources:
      limits:
        cpu: "0.5"
        memory: 1G
      requests:
        cpu: "0.5"
        memory: 1G
